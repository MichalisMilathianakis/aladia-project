# Aladia Project (Full Stack)

**Frontend:** Next.js + TailwindCSS v4 
**Backend:** NestJS Monorepo (Gateway + Auth microservice)

Run locally:
```bash
# Start backend
cd backend && npm run start:dev:gateway
cd backend && npm run start:dev:authentication

# Start frontend
cd frontend && npm run dev
